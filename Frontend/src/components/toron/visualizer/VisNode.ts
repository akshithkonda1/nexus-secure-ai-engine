export interface VisNode {
  x: number;
  y: number;
  radius: number;
  baseRadius: number;
  driftOffsetX: number;
  driftOffsetY: number;
  orbitAngle: number;
  orbitSpeed: number;
  orbitRadius: number;
}
