apiVersion: v1
kind: Service
metadata:
  name: {{ include "ryuzen-engine.fullname" . }}
  labels:
    {{- include "ryuzen-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "ryuzen-engine.selectorLabels" . | nindent 4 }}
