version: "3.9"

services:
  ryuzen-engine:
    build: .
    container_name: ryuzen-engine
    restart: always
    ports:
      - "8080:8080"
    environment:
      ENVIRONMENT: "production"
      LOG_LEVEL: "info"
    volumes:
      - .:/app
